# Battrick NZ dashboard — publish checkout

Static single-file build of the NZ national-team analytics dashboard.
Built by `scripts/build_share.py` in the Battrick project and pushed here by
`scripts/publish_share.py`; deployed to GitHub Pages by the Actions workflow
(same retry pattern as the TDF dashboard).

Live: https://acthecoder88.github.io/battrick-nz-dashboard/
